apiVersion: v1
kind: Service
metadata:
  name: {{ include "pubg-map.fullname" . }}
  labels:
    {{- include "pubg-map.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pubg-map.selectorLabels" . | nindent 4 }}
